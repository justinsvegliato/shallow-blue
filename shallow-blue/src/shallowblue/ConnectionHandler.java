package shallowblue;

public class ConnectionHandler {
  public static void main(String[] args) throws InterruptedException {
    while (true) {
      Thread.sleep(5000);
    }
  }
 
  private byte[] interpretLiteralMove(String move) {
    //char[] components = move.toCharArray();
    //byte from = components[]
    return null;
  }
}
